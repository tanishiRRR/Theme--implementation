<div>
  <h2>商品一覧</h2>
  <p>(全<%= @items.count %>件)</p>
</div>

<div>
  <% @items.each do |item| %>
  <%= image_tag item.get_image %><br>
  <%= item.name %><br>
  ￥<%= item.price.to_s(:delimited) %><br>
  <% end %>
</div>

<%= paginate @items %>